# 🚀 STEPAN VPN — Полная инструкция по развёртыванию

## 📋 Что нужно

1. **VPS сервер** (Ubuntu/Debian) за границей — Hetzner, DigitalOcean, Vultr
2. **GitHub аккаунт** — для деплоя на Vercel
3. **Vercel аккаунт** — бесплатный хостинг панели
4. **Telegram бот** — уже создан: @stepavpnbot

---

## ☁️ Вариант 1: Деплой на Vercel (рекомендуется)

### Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на [github.com](https://github.com) и создайте новый репозиторий
2. Назовите его `stepan-vpn` (или любое другое имя)
3. Сделайте его **Private** для безопасности

### Шаг 2: Загрузите файлы

```bash
cd xray-panel
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/stepan-vpn.git
git push -u origin main
```

### Шаг 3: Деплой на Vercel

1. Зайдите на [vercel.com](https://vercel.com) и войдите через GitHub
2. Нажмите **"Add New Project"**
3. Выберите репозиторий `stepan-vpn`
4. В настройках проекта:
   - **Framework Preset**: Other
   - **Root Directory**: оставьте пустым
5. Нажмите **"Deploy"**

### Шаг 4: Настройте переменные окружения

В Vercel Dashboard → Settings → Environment Variables добавьте:

| Переменная | Значение | Описание |
|------------|----------|----------|
| `ADMIN_PASSWORD` | `Artem1522@` | Пароль для входа в админку |
| `SECRET_KEY` | `your-random-secret-key` | Секретный ключ для сессий |
| `VPN_SERVER_IP` | `123.45.67.89` | IP вашего VPN сервера |
| `VPN_PUBLIC_KEY` | `YMP9n4VQrq...` | Публичный ключ Reality |
| `VPN_SHORT_ID` | `4efdffa441b46214` | Short ID |
| `VPN_SERVER_NAME` | `io.ozone.ru` | Сайт маскировки |

### Шаг 5: Получите URL панели

После деплоя Vercel выдаст URL вида: `https://stepan-vpn.vercel.app`

Этот URL используйте для:
- Входа в админку
- Настройки бота (переменная `PANEL_URL`)

---

## 🖥️ Вариант 2: Локальный запуск (для теста)

```bash
cd xray-panel
pip install -r requirements.txt
python app.py
```

Панель: http://127.0.0.1:5000
Пароль: `Artem1522@`

---

## 🔧 Настройка VPN сервера

### Автоматически через админку

1. Откройте панель и войдите с паролем
2. В разделе "Настройка сервера" введите:
   - IP адрес VPS
   - SSH порт (22)
   - Пользователь (root)
   - Пароль от сервера
   - Выберите сайт маскировки
3. Нажмите "Настроить сервер"
4. Дождитесь завершения — панель автоматически:
   - Установит Xray
   - Сгенерирует ключи Reality
   - Настроит конфигурацию
   - Запустит VPN

### Вручную (если нужно)

```bash
# На VPN сервере
bash -c "$(curl -L https://github.com/XTLS/Xray-install/raw/main/install-release.sh)" @ install

# Генерация ключей
xray x25519
# Запишите Private key и Public key

# Генерация shortId
openssl rand -hex 8
```

После ручной настройки добавьте ключи в переменные окружения Vercel.

---

## 🤖 Telegram бот

### Настройка бота

1. Откройте файл `bot.py`
2. Измените `PANEL_URL` на URL вашей панели:
```python
PANEL_URL = os.environ.get('PANEL_URL', 'https://stepan-vpn.vercel.app')
```

### Запуск бота на VPS

```bash
# На любом сервере (можно на том же VPN сервере)
cd xray-panel
pip install -r requirements.txt
export PANEL_URL="https://stepan-vpn.vercel.app"
python bot.py
```

### Запуск как systemd сервис

```bash
cat > /etc/systemd/system/stepan-bot.service << 'EOF'
[Unit]
Description=STEPAN VPN Telegram Bot
After=network.target

[Service]
User=root
WorkingDirectory=/root/xray-panel
Environment="PANEL_URL=https://stepan-vpn.vercel.app"
ExecStart=/usr/bin/python3 bot.py
Restart=always

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable stepan-bot
systemctl start stepan-bot
```

---

## 📱 Как пользоваться

### Для администратора:
1. Откройте панель: `https://your-panel.vercel.app`
2. Войдите с паролем `Artem1522@`
3. Настройте VPN сервер (один раз)
4. Добавляйте клиентов через форму

### Для пользователей:
1. Напишите боту @stepavpnbot
2. Нажмите "Получить подписку"
3. Откройте страницу подписки
4. Нажмите "Подключить VPN" — HAPP откроется автоматически
5. Готово!

---

## 🔒 Безопасность

### На VPN сервере:
```bash
ufw allow 22/tcp
ufw allow 443/tcp
ufw enable
```

### Рекомендации:
- Используйте сложный пароль для админки
- Сделайте GitHub репозиторий приватным
- Регулярно обновляйте Xray на сервере

---

## 📝 Переменные окружения (полный список)

| Переменная | Обязательно | Описание |
|------------|-------------|----------|
| `ADMIN_PASSWORD` | ✅ | Пароль админки |
| `SECRET_KEY` | ✅ | Секрет для сессий |
| `VPN_SERVER_IP` | ✅ | IP VPN сервера |
| `VPN_PUBLIC_KEY` | ✅ | Публичный ключ Reality |
| `VPN_SHORT_ID` | ✅ | Short ID |
| `VPN_SERVER_NAME` | ❌ | Сайт маскировки (по умолчанию io.ozone.ru) |
| `PANEL_URL` | ❌ | URL панели (для бота) |

---

## 🆘 Решение проблем

### Панель не открывается на Vercel
- Проверьте логи в Vercel Dashboard → Deployments → Logs
- Убедитесь что все переменные окружения заданы

### Бот не отвечает
```bash
systemctl status stepan-bot
journalctl -u stepan-bot -f
```

### VPN не подключается
1. Проверьте порт 443 на VPN сервере: `ufw status`
2. Проверьте Xray: `systemctl status xray`
3. Проверьте логи: `journalctl -u xray -f`

### Ошибка авторизации
- Убедитесь что `ADMIN_PASSWORD` задан в переменных окружения
- Очистите cookies браузера

---

## 🎉 Готово!

Ваш премиум VPN сервис готов к работе!

- 🌐 Панель: https://your-panel.vercel.app
- 🤖 Бот: @stepavpnbot
- 📱 Клиенты подключаются через HAPP

⚡ STEPAN VPN — Premium VPN Service
